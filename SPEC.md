the spec to implement EIP-7805 is in the file eip7805.md in this folder
if there are not unit tests for the new engine methods defined in the spec, create them
follow existing patterns for prior versions of engine api tests
if the tests do not cover all cases described in the spec, improve them.
add implementations for the new methods, following existing patterns for prior versions of engine api tests.
if the tests do not pass, refine the implementations till they do.
stop when the tests pass or your context window hits 100k tokens.
